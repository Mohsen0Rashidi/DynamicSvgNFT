This is the NFT project that hosted 100% onchain with uses the Base64 for encode the metadata and uses the Chainlink Price Feed to mint the NFT depend on the price.

if price is less than the high value, the fowny NFT will mint otherwise it will mint the happy NFT